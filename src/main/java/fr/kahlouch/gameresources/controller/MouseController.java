package fr.kahlouch.gameresources.controller;

import javafx.scene.input.MouseEvent;

public class MouseController extends Controller<MouseEvent> {
    public MouseController(int maxPending) {
        super(maxPending);
    }
}
